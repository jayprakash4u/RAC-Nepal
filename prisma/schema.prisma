// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "sqlserver"
}

// An admin account. Password is stored as a bcrypt hash, never plaintext.
model AdminUser {
  id           String    @id @default(cuid())
  username     String    @unique @db.NVarChar(100)
  passwordHash String    @db.NVarChar(200)
  createdAt    DateTime  @default(now())
  lastLoginAt  DateTime?
  sessions     Session[]

  @@map("admin_users")
}

// A logged-in admin session. The cookie holds `token`; only its bcrypt-free
// SHA-256 hash is stored here so a DB leak alone can't be replayed as a cookie.
model Session {
  id        String    @id @default(cuid())
  tokenHash String    @unique @db.NVarChar(200)
  userId    String
  user      AdminUser @relation(fields: [userId], references: [id], onDelete: Cascade)
  createdAt DateTime  @default(now())
  expiresAt DateTime

  @@map("admin_sessions")
}

// A request submitted through the site's "Book an Appointment" form.
model AppointmentRequest {
  id              String    @id @default(cuid())
  name            String    @db.NVarChar(200)
  phone           String    @db.NVarChar(50)
  email           String    @db.NVarChar(200)
  appointmentType String    @db.NVarChar(50)
  preferredDate   DateTime? @db.Date
  preferredTime   String?   @db.NVarChar(50)
  message         String?   @db.NVarChar(2000)
  source          String?   @db.NVarChar(100)
  status          String    @default("new") @db.NVarChar(20) // new | contacted | completed | cancelled
  emailSent       Boolean   @default(false)
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt

  @@map("appointment_requests")
}

model GalleryImage {
  id        String   @id @default(cuid())
  src       String   @db.NVarChar(500)
  alt       String   @db.NVarChar(300)
  order     Int      @default(0)
  createdAt DateTime @default(now())

  @@map("gallery_images")
}

model Testimonial {
  id        String   @id @default(cuid())
  quote     String   @db.NVarChar(2000)
  name      String   @db.NVarChar(200)
  role      String   @db.NVarChar(200)
  initials  String   @db.NVarChar(10)
  imageSrc  String?  @db.NVarChar(500)
  imageAlt  String?  @db.NVarChar(300)
  order     Int      @default(0)
  createdAt DateTime @default(now())

  @@map("testimonials")
}

model Video {
  id           String   @id @default(cuid())
  youtubeId    String   @db.NVarChar(50)
  title        String   @db.NVarChar(300)
  category     String   @db.NVarChar(50)
  startSeconds Int?
  order        Int      @default(0)
  createdAt    DateTime @default(now())

  @@map("videos")
}

// `sections` holds the article body as JSON (paragraph/heading/list/callout
// blocks) — a JSON column is a better fit here than normalizing each block
// type into its own table for how this content is actually authored and read.
model BlogPost {
  id          String   @id @default(cuid())
  slug        String   @unique @db.NVarChar(200)
  title       String   @db.NVarChar(300)
  excerpt     String   @db.NVarChar(1000)
  category    String   @db.NVarChar(100)
  publishedAt DateTime @db.Date
  readTime    String   @db.NVarChar(50)
  imageSrc    String   @db.NVarChar(500)
  imageAlt    String   @db.NVarChar(300)
  sections    String   @db.NVarChar(Max)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@map("blog_posts")
}
